# ================  IMAP ====================
set imap_user = 'jlcrome@g.clemson.edu'
set imap_pass = ""
set spoolfile = imaps://imap.gmail.com/INBOX
set folder = imaps://imap.gmail.com/
set record="imaps://imap.gmail.com/[Gmail]/Sent Mail"
set postponed="imaps://imap.gmail.com/[Gmail]/Drafts"
set mbox="imaps://imap.gmail.com/[Gmail]/All Mail"
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~/.mutt/certificates"

# ================  SMTP  ====================
set ssl_starttls = yes
set ssl_force_tls = yes # Require encrypted connection
set smtp_url="smtps://jlcrome@g.clemson.edu@smtp.gmail.com:465/"
set smtp_pass = $imap_pass
set smtp_authenticators = "login"

# ================  Composition  ====================
set editor = "vim"      # Set your favourite editor.
set edit_headers = yes  # See the headers when editing
set charset = UTF-8     # value of $LANG; also fallback for send_charset
# Sender, email address, and sign-off line must match
unset use_domain        # because joe@localhost is just embarrassing
unset record		# gmail auto saves set email to [Gmail]/Sent
set realname='Justin Cromer'
set from = "jlcrome@g.clemson.edu"
set use_from = yes
